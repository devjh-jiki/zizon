---
"@zizon/skills": minor
---

Consolidate 35 skills across 5 buckets into 19 skills across 7 buckets (`token`, `design`, `planning`, `review`, `testing`, `learning`, `util`), ship them as a single `zizon` Claude Code plugin (one `plugin.json` with an explicit 19-path `skills` array, one `marketplace.json` entry), and add a `bootstrap/` layer that declares the desired plugin/marketplace/MCP-server state for fresh-machine setup.
