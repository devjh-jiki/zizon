{
  "name": "@dev-hub/skills",
  "version": "0.1.0",
  "private": true,
  "description": "공용 에이전트 스킬과 런타임별 어댑터 모음 - 버저닝 + upstream 동기화",
  "license": "MIT",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset tag",
    "validate": "node scripts/validate.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
